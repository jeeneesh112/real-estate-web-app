import { Card, CardContent, Typography } from '@mui/material';

export const TowerCard: React.FC = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Tower Card</Typography>
        <Typography variant="body2">Placeholder</Typography>
      </CardContent>
    </Card>
  );
};

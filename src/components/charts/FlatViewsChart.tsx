import { Box, Typography } from '@mui/material';

export const FlatViewsChart: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Flat Views Chart</Typography>
      <Typography variant="body2">Chart placeholder</Typography>
    </Box>
  );
};

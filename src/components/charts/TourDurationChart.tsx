import { Box, Typography } from '@mui/material';

export const TourDurationChart: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Tour Duration Chart</Typography>
      <Typography variant="body2">Chart placeholder</Typography>
    </Box>
  );
};

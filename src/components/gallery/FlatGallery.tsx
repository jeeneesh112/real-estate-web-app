import { Box, Typography } from '@mui/material';

export const FlatGallery: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Flat Gallery</Typography>
      <Typography variant="body2">Placeholder</Typography>
    </Box>
  );
};

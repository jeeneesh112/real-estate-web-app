import { Box, Typography } from '@mui/material';

export const ImageSlider: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Image Slider</Typography>
      <Typography variant="body2">Placeholder</Typography>
    </Box>
  );
};

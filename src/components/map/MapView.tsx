import { Box, Typography } from '@mui/material';

export const MapView: React.FC = () => {
  return (
    <Box sx={{ height: 400, backgroundColor: '#e0e0e0', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <Typography variant="h6">Google Maps Placeholder</Typography>
    </Box>
  );
};
